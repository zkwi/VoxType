#!/bin/sh

# 提交前做一次本地 secret 粗扫，拦住明显的误提交。
if command -v node >/dev/null 2>&1; then
  node scripts/scan-secrets.mjs --staged
  exit $?
fi

echo "[pre-commit] Node.js not found, skip secret scan" >&2
exit 0
